// Деструктурируйте объект с пользователем, получите имя и возраст.
const user = {
    name: "Айсулу",
    age: 22,
    city: "Алматы"
};

// Из массива [10, 20, 30, 40] получите первые два элемента и остальные отдельно.

// Объедините два объекта a = {x:1}, b = {y:2} в один.

// Создайте функцию с ...args, которая считает сумму всех переданных чисел.

// Используя деструктуризацию, из объекта вытащи language, остальные положи в otherSettings.
settings = { 
    theme: "dark", 
    language: "kz", 
    fontSize: 14 
}

// Напиши функцию parseJSON(str), которая пытается распарсить JSON-строку. 
// Используй try/catch, чтобы вернуть "Ошибка парсинга" при недопустимом JSON.

// Примеры:
console.log(parseJSON('{"name": "Ali"}'));      // { name: "Ali" }
console.log(parseJSON('{name: "Ali"}'));        // "Ошибка парсинга"

// Реализуй readUser(user), которая принимает объект с полем name. 
// Если поля нет — выбрось ошибку "Имя не указано" и обработай её.

// Раздели taskForModules.js на модули (math.js, user.js, main.js)

// Модуль	        Файл	    Что содержит
// Калькулятор	    math.js	    add, subtract, multiply, divide
// Пользователи	    user.js	    users, getUserNames, getAdultUsers
// Точка входа	    main.js	    Импорты, выполнение логики, вывод результатов

